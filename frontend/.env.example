# Copy to `frontend/.env` and customize for your local environment.
# All variables prefixed with `VITE_` are exposed to the browser bundle.
# Never place API keys, passwords, or any other secrets in this file.
VITE_API_BASE=/api
VITE_DEV_BACKEND=http://localhost:8000
