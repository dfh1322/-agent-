/**
 * theme/index.ts — 单一入口。
 * 各页面应使用 ``import { palette, space, text } from '@/theme'``。
 */
export { palette } from './colors';
export { space, radius, fontSize, fontWeight, motion, layout } from './spacing';
export { text, safeText } from './typography';
